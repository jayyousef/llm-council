// Runtime-injected config for the deployed nginx container.
// In local `vite dev`, this file is served from `public/` and usually stays empty;
// in production, nginx overwrites `/config.js` at container start.
window.__LLM_COUNCIL_CONFIG__ = window.__LLM_COUNCIL_CONFIG__ || {};

